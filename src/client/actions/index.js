
export const Thunk = {
  //someAction : (router, params) => (dispatch, getState) => {
  //  dispatch({
  //
  //  });
  //
  //  someAsyncAction(params, (err, files) => {
  //    dispatch({
  //
  //    });
  //  });
  //},

}
